# Native Linux/x86-64 runtime parity goal

## Status and boundary

This is the active staged program for native Linux/x86-64 little-endian
support. It is not a claim that `crabc`, `crabc-libc`, `crabc-ldso`, or
`crabc-rs` supports x86-64 today. Until its promotion gates pass, the public
support boundary remains Linux/AArch64 little-endian as stated in `SCOPE.md`,
`COMPATIBILITY-PROFILE.md`, and `STATUS.md`.

The target is Linux 5.10 or newer, native x86-64 execution only. Do not use
AArch64 emulation as evidence, add a generic portability framework, or broaden
this goal to another architecture, endianness, kernel, or operating system.

## Completion accounting

[`compat/x86_64/parity.toml`](compat/x86_64/parity.toml) is the closed,
machine-readable x86 parity ledger. It maps the selected AArch64 capability
and gate surface into ordered x86 families, their ABI/header prerequisites,
native evidence, oracle, and promotion state. Its validator rejects missing,
duplicate, or stale accounting; passing a narrow foundation command never
changes the public support boundary. This document is the readable goal and
dependency map; the ledger is the completion ratchet.

### Current verified foundation

The closed `./scripts/dev-x86_64.sh musl-oracle` command source-builds and
verifies pinned musl 1.2.6 beneath `/opt/musl-1.2.6`, including its compiler
specification, x86 interpreter, and mapped runtime `libc.so`. It is oracle
toolchain provenance only: it is neither a crabc artifact nor a compatibility
differential result.

The closed `./scripts/dev-x86_64.sh header-abi-reference` command records the
pinned x86 SysV LP64/x87 C baseline: 16-byte storage/alignment for 80-bit
extended `long double`, the corresponding complex calling convention, and
the 32-byte musl `fenv_t`. It deliberately compiles no crabc public header;
the reference baseline itself is not public x86 C-header support.

The separate `./scripts/dev-x86_64.sh header-abi-project` command places the
project include directory first and compile-checks the source-only x86
`fenv`, `float`, and fundamental-type declarations against that baseline in
both SSE and x87 evaluation modes. It has no link step and does not select
`crabc-libc`: all remaining installed headers and C runtime behavior remain
unadmitted until their own target-specific contracts have evidence.

The separate `./scripts/dev-x86_64.sh sys-reg-header-abi` command records the
27 x86 ptrace register-index macros in the project `<sys/reg.h>` against the
pinned musl declaration oracle. It is a compile-only declaration slice; it
does not provide ptrace behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh types-header-abi` command checks the
staged `<bits/alltypes.h>` and `<sys/types.h>` C and C++ declarations against
pinned musl: `nlink_t`, signed `blksize_t`, C's opaque-pointer `pthread_t`,
C++'s integer `pthread_t`, and the named opaque pthread object layouts. It is
compile-only and does not establish pthread behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh stat-header-abi` command checks the
staged C and C++ `<sys/stat.h>` declarations against pinned musl, including
the 144-byte x86 `struct stat` field map, selected mode declarations, and
timestamp constants. It is compile-only and does not provide filesystem
behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh time-header-abi` command checks the
staged C and C++ `<time.h>` declarations against pinned musl: LP64 time types,
`timespec`, `itimerspec`, `tm`, GNU aliases, clocks, and selected timer
signatures. It is compile-only and does not provide C time behavior or select
`crabc-libc`.

The separate `./scripts/dev-x86_64.sh poll-header-abi` command checks the
staged C and C++ `<poll.h>` declarations against pinned musl: `nfds_t`,
`pollfd`, `poll`/`ppoll`, and x86 extension values. It is compile-only and
does not provide polling behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh fcntl-header-abi` command checks the
staged C and C++ `<fcntl.h>` declarations against pinned musl: x86 open and
fcntl flags, `flock`, GNU owner/file-handle records, selected extensions, and
large-file aliases including `lockf64`. It is compile-only and does not provide
descriptor behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh unistd-header-abi` command checks the
staged x86 LP64 C and C++ `<unistd.h>` declarations against pinned musl:
POSIX/GNU selectors, process/system helpers, lock constants, and large-file
aliases. It is compile-only and does not provide C process, filesystem, or
descriptor behavior or select `crabc-libc`; x32 is not admitted by this LP64
slice.

The separate `./scripts/dev-x86_64.sh system-header-abi` command checks the
staged C and C++ `<sys/utsname.h>` and `<sys/sysinfo.h>` declarations against
pinned musl, including the public 368-byte `struct sysinfo` compatibility
tail. It is compile-only and does not provide C system-information behavior or
select `crabc-libc`; the typed Rust system slice separately owns only Linux's
112-byte syscall output prefix.

The separate `./scripts/dev-x86_64.sh syscall-header-abi` command compares all
384 `__NR_*` and 384 `SYS_*` project `<sys/syscall.h>` macros with pinned musl
1.2.6. It is a declaration-only source slice; a complete numeric namespace
does not provide syscall wrappers or a C runtime artifact.

The separate `./scripts/dev-x86_64.sh signal-header-abi` command checks the
staged GNU and POSIX `<signal.h>` x86 signal-frame declarations against pinned
musl: `sigset_t`, `sigaction`, alternate-stack, general-register, floating
state, `sigcontext`, `mcontext_t`, and `ucontext_t` layouts. It is compile-only
and does not provide C signal behavior or select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh mman-header-abi` command checks the
staged C and C++ `<sys/mman.h>` declarations and selected mapping, protection,
advice, remap, memory-lock, and x86 `MAP_32BIT` values against pinned musl. It
is a declaration-only source slice; it does not provide C mapping behavior or
select `crabc-libc`.

The separate `./scripts/dev-x86_64.sh mm-abi-reference` command pins the musl
x86 `mmap`/`mremap`/`mprotect`/`munmap` numbers and the closed mapping and
remapping flag constants used by the native Rust mapping slice. It neither
compiles project headers nor selects a C ABI artifact.

The separate `./scripts/dev-x86_64.sh mlock-reference` command pins x86
`mlock`/`munlock`/`mlock2` syscall values, `MLOCK_ONFAULT`, constrained-memlock
outcomes, and invalid range/flag behavior. It supports only typed Rust
per-range locking; it neither selects a C ABI artifact nor admits process-wide
`mlockall`/`munlockall` policy.

The separate `./scripts/dev-x86_64.sh msync-reference` command pins the x86
`msync` syscall value, accepted `MS_*` flag combinations, and zero-length
behavior. It is evidence only for the typed Rust mapping-synchronization
operation; it neither selects a C ABI artifact nor admits a wider mapping
policy.

The separate `./scripts/dev-x86_64.sh madvise-reference` command pins the x86
`madvise` syscall value and advice vocabulary, including Linux private-anonymous
`MADV_DONTNEED` page discard and musl-compatible POSIX `DONTNEED` no-op
behavior. It supports only the named typed Rust advisory operations, not a C
mapping ABI or wider VM policy.

The separate `./scripts/dev-x86_64.sh mincore-reference` command pins the x86
`mincore` syscall value, 4096-byte page unit, one-byte-per-page output, and
partial-range rounding. It is evidence only for the bounded typed Rust
residency query, not C mapping support or a general VM facade.

The separate `./scripts/dev-x86_64.sh fs-advice-reference` command pins x86
`fadvise64` and `readahead` syscall values, all six `POSIX_FADV_*` policies,
descriptor-position preservation, and direct invalid range/descriptor behavior.
It is evidence only for the typed Rust `fs::{fadvise, readahead}` slice, not C
filesystem support or broader path-based filesystem behavior.

The separate `./scripts/dev-x86_64.sh memfd-reference` command records the
direct typed x86 anonymous memory-file and sealing slice. It pins
`memfd_create=319`, raw `fcntl=72`, the closed
`MFD_CLOEXEC`/`MFD_ALLOW_SEALING`/`MFD_HUGETLB` values `1`/`2`/`4`,
`F_ADD_SEALS=1033`, `F_GET_SEALS=1034`, and Linux-5.10
`F_SEAL_SEAL`/`SHRINK`/`GROW`/`WRITE`/`FUTURE_WRITE` values
`1`/`2`/`4`/`8`/`16`. It verifies named fresh-descriptor ownership and
`CLOEXEC`, the 249-byte accepted versus 250-byte-kernel-`EINVAL` label
boundary, sealing-capable/plain state, `F_SEAL_WRITE`'s live-writable-mapping
`EBUSY` guard followed by write rejection, grow/shrink effects, and the
`F_SEAL_FUTURE_WRITE` distinction: writable shared mappings made before the
seal remain usable, while direct writes and later writable shared mappings get
`EPERM`. It also verifies final-seal `EPERM` and pipe/closed-descriptor errors.
The Rust regression separately proves that its 256-byte fixed-stack input
fails with `NAMETOOLONG` before the syscall.

This completes typed Rust `filesystem.memory-file`,
`filesystem.seal-observation`, and `filesystem.seal-mutation`, not a C
`fcntl`/header ABI or broader filesystem behavior. `MFD_HUGETLB` selects only
the kernel default huge-page size; huge-page sizing/reservation policy and
`memfd_secret` remain excluded. `SealFlags::EXEC` is retained and directly
forwarded for newer kernels, but its Linux-6.3 executable-policy semantics are
unproved on the Linux-5.10 baseline.

The separate `./scripts/dev-x86_64.sh time-abi-reference` command pins the
musl x86 `timespec` shape, admitted realtime/monotonic/monotonic-raw/process-
CPU clock IDs, and `clock_gettime`/`clock_getres` syscall values used by the
bounded Rust clock slice. It neither compiles project headers nor selects a C
ABI artifact.

The separate `./scripts/dev-x86_64.sh time-observation-reference` command
records pinned-musl x86 normalized realtime, C `time(NULL)` whole-second
behavior, and nondecreasing process-CPU clock observations used by typed
`time`, `timespec_get`, `realtime_millis`, and `process_cpu_time` helpers. It
neither compiles project headers nor selects a C ABI artifact.

The separate `./scripts/dev-x86_64.sh relative-sleep-reference` command pins
musl x86 `nanosleep(2)` zero-duration completion, invalid-request
`EINVAL`, and signal-interrupted positive-remainder behavior. It is evidence
only for the typed Rust relative-sleep result; it neither selects C
`nanosleep`/`sleep`/`usleep` behavior nor a C ABI artifact.

The separate `./scripts/dev-x86_64.sh clock-nanosleep-reference` command
records the direct typed x86 `clock_nanosleep(2)` slice. It pins syscall 230
and the 16-byte, align-8 `timespec`, verifies relative zero completion and a
child-contained `EINTR` positive remainder, and proves both a completed past
deadline and signal-interrupted `TIMER_ABSTIME` deadline pass a null remainder
pointer. Pinned musl's C function returns direct positive errors, while the
raw syscall exposes the normal `-1` plus `errno` convention; the typed Rust
facade exposes neither C convention nor TLS `errno`. This completes only
`time.clock-sleep`: C sleep APIs, clock mutation, POSIX timers, and broader
time policy remain excluded.

The separate `./scripts/dev-x86_64.sh getitimer-reference` command executes
the direct typed x86 read-only interval-timer query. It pins signed 16-byte,
align-8 `timeval` and 32-byte, align-8 `itimerval` layouts (nested offsets zero
and eight; interval/value offsets zero and 16), `getitimer=36`, and all three
`ITIMER_*` selectors. Both musl and direct-syscall results are independently
validated without comparing successive values that can naturally decrement.
It admits only `time::getitimer`; C time APIs, timer/signal delivery policy,
and general x86 facade support remain excluded.

The separate `./scripts/dev-x86_64.sh setitimer-reference` command records the
x86 `time.process-interval-control` slice. It pins `setitimer=38` over the
established 16-byte `timeval` and 32-byte `itimerval` records, all three
`ITIMER_*` selectors, and a complete old/new/disarm exchange plus malformed-
microsecond `EINVAL` behavior for both pinned musl and the raw syscall; every
stateful operation runs in a short-lived child. The Rust facade admits
validated microsecond settings and the Rust-only `alarm`/`ualarm` aliases,
which operate on `ITIMER_REAL`: `alarm` ceilings a prior fractional remainder
to seconds, while `ualarm` reports bounded whole microseconds. The pinned-musl
C `ualarm` reference is valid only for subsecond inputs because musl does not
normalize inputs of one second or more; its exact 1,000,000-microsecond
boundary returns `EINVAL` without a usable C failure return. The Rust facade
intentionally accepts `u32` microseconds through `Duration`; these aliases add
no C ABI. C time APIs,
timer/signal delivery policy, and broader timer control remain excluded.

The separate `./scripts/dev-x86_64.sh timerfd-reference` command executes
pinned-musl and raw x86 proofs for direct typed
`time::{timerfd_create, timerfd_settime, timerfd_gettime}`. It pins the
32-byte, align-8 `itimerspec` record (interval/value offsets zero and 16),
`timerfd_create=283`, `timerfd_settime=286`, `timerfd_gettime=287`, all five
named Linux timer clock values (preserving the alarm-clock capability result),
known and future-bit kernel validation, close-on-exec/nonblocking creation,
relative/absolute settings, `CANCEL_ON_SET` acceptance, periodic-setting
inspection, exact eight-byte expiration reads, disarming, and representative
invalid cases. It does not
promote C time APIs, wider timer policy, or general x86 facade support.

The separate `./scripts/dev-x86_64.sh pselect-reference` command executes
pinned-musl and raw x86 descriptor-bit-vector proofs for the direct typed
`event::{select, pselect}` slice. It pins `FD_SETSIZE=1024`, the 128-byte
`fd_set` with eight-byte words, `pselect6=270`, empty/readable pipe behavior,
caller-timeout preservation, invalid `nfds` handling, raw mask-pointer/size
argument-six placement, and raw/pinned-musl temporary signal-mask restoration. C
select APIs and a general x86 facade remain excluded.

The separate `./scripts/dev-x86_64.sh poll-reference` command executes a
pinned-musl x86 pipe fixture through `poll(2)`, pinning empty readiness,
readability, and writer-close hangup behavior for the bounded typed Rust poll
slice. It neither compiles a project C header nor selects a C ABI artifact.

The separate `./scripts/dev-x86_64.sh ppoll-reference` command executes a
pinned-musl x86 pipe and signal fixture through `ppoll(2)` and `pause(2)`,
pinning readiness, temporary signal-mask restoration, and `EINTR` completion.
It is an oracle for only the typed Rust `ppoll`/`pause` slice, not public C
polling support or `crabc-libc` selection.

The separate `./scripts/dev-x86_64.sh epoll-reference` command executes
pinned-musl and raw x86 epoll lifecycle proofs for the direct typed
`event::epoll` slice. It pins the packed 12-byte, align-1 `epoll_event` record
(event bits at offset zero and its 64-bit data union at offset four),
`epoll_create1=291`, `epoll_ctl=233`, `epoll_pwait=281`, close-on-exec/legacy
creation, future-bit forwarding for Linux validation, empty/add/modify/delete
readiness behavior, borrowed eight-byte masks, and temporary mask
installation/restoration. C polling support and general x86 facade support
remain excluded.

The separate `./scripts/dev-x86_64.sh process-identity-reference` command
executes pinned-musl x86 scalar and real/effective/saved UID/GID observations.
It is an oracle for the bounded typed Rust read-only identity slice, not C
process API or `crabc-libc` support.

The separate `./scripts/dev-x86_64.sh getgroups-reference` command executes a
pinned-musl x86 supplementary-group query/fill lifecycle. It pins unsigned
32-bit, align-4 `gid_t`, `getgroups=115`, null zero-count queries,
musl/direct fill equivalence, and the conditional undersized-buffer `EINVAL`
result. It admits only typed Rust `process::{getgroups_count, getgroups}`:
the count is a sizing observation rather than a reservation, so callers retry
the pair after `EINVAL` when the credential snapshot changes. It does not
admit C `getgroups`/`setgroups`, credential mutation or synchronization, or
general x86 facade support.

The separate `./scripts/dev-x86_64.sh process-session-reference` command
executes pinned-musl x86 `getpgid`, `getpgrp`, and `getsid` observations. It is
an oracle for the typed optional-PID process-group/session slice, not process
creation, session mutation, C process API, or `crabc-libc` support.

The separate `./scripts/dev-x86_64.sh umask-reference` command executes a
child-contained pinned-musl x86 process-mask exchange. It pins `umask=95`,
unsigned 32-bit `mode_t`, and raw/musl old-mask exchange plus restoration for
the typed `process::umask` operation. It admits neither pathname creation nor
a C process API.

The separate `./scripts/dev-x86_64.sh pidfd-open-reference` command executes
pinned-musl x86 `pidfd_open(2)` calls, pinning fresh descriptor ownership,
`PIDFD_NONBLOCK`, and direct missing-target/invalid-flag errors. It is an
oracle for only the typed Rust pidfd-creation slice, not process control, C
process API, or `crabc-libc` support.

The separate `./scripts/dev-x86_64.sh fcntl-getlk-reference` command pins the
x86 32-byte `struct flock` layout, `F_GETLK`, and the unlocked/conflicting
record-lock query behavior, including a forked parent-lock observation. It is
evidence only for the typed Rust read-only `fcntl_getlk` query; lock mutation,
general `fcntl`, C process APIs, and `crabc-libc` remain unadmitted.

The separate `./scripts/dev-x86_64.sh fcntl-status-reference` command
completes the direct typed Rust `fs::{OFlags, fcntl_getfl, fcntl_setfl}` status
flag slice. It pins x86 `fcntl=72`, `F_GETFL=3`, `F_SETFL=4`, the x86
`O_ACCMODE=0x00200003` form plus mutable status-bit encodings, raw/pinned-musl
shared-open-file-description mutation across `dup`, immutable
access/creation/per-descriptor bits, exact restoration, and closed-descriptor
`EBADF`. It does not admit `open`/`openat`, generic C `fcntl`, errno TLS, or
path behavior.

The separate `./scripts/dev-x86_64.sh flock-reference` command admits only
typed `fs::{FlockOperation, flock}` whole-file advisory locking. It records
x86 syscall `73` and the closed `LOCK_SH`/`LOCK_EX`/`LOCK_NB`/`LOCK_UN`
values `1`/`2`/`4`/`8`: locks are associated with an open file
description, so duplicate descriptors share and release that state, while a
separately opened child descriptor's nonblocking exclusive request conflicts
then succeeds after release. Raw and pinned-musl requests agree on direct
invalid-operation `EINVAL` and closed-descriptor `EBADF`. This admits
neither `flock`/`fcntl` record-lock interaction nor `fcntl` record-lock
mutation, C APIs/errno TLS, pathname opening, durability, nor
network/distributed-filesystem semantics.

The separate `./scripts/dev-x86_64.sh sendfile-reference` command admits
only typed `fs::sendfile` descriptor transfer. It records x86 syscall `40`
and signed 64-bit `off_t`: an explicit input offset advances while preserving
the input descriptor's shared position, whereas a null offset advances that
shared position. Its unlinked regular-file fixture proves short and EOF-zero
transfers, output bytes and position, plus raw/pinned-musl invalid-offset
`EINVAL` and closed-descriptor `EBADF`. This admits neither C APIs/errno
TLS, pathname opening, socket/network or splice semantics, durability, nor
kernel descriptor ownership transfer. Passing a reference or `BorrowedFd`
retains Rust descriptor ownership; an owning `AsFd` passed by value follows
ordinary Rust move/drop semantics.

The separate `./scripts/dev-x86_64.sh copy-file-range-reference` command
admits only typed `fs::copy_file_range` descriptor-range copying. It records
x86 syscall `326` and signed 64-bit `off_t`: independently optional explicit
input/output offsets are staged and commit only after a successful copy without
moving either descriptor's shared position, while null offsets advance their
respective shared positions. Its same-filesystem unlinked regular-file fixture
proves short and EOF-zero transfers at fixed zero flags, and compares raw with
pinned-musl explicit-pointer behavior. The C probe records raw/pinned-musl
negative-offset `EOVERFLOW`, nonzero-flag `EINVAL`, and closed-descriptor
`EBADF`; the typed Rust boundary rejects unrepresentable unsigned ranges with
`Errno::INVAL` before either `AsFd` conversion. This admits neither C APIs nor
errno TLS, pathname operations, copy flags, sendfile/splice fallbacks,
filesystem-copy policy, nor durability.

The separate `./scripts/dev-x86_64.sh scheduler-priority-bounds-reference`
command pins x86 `SCHED_OTHER`/`SCHED_FIFO`/`SCHED_RR` values, the
`sched_get_priority_max`/`sched_get_priority_min` syscall numbers, their stable
priority bounds, and invalid-policy behavior. It is evidence only for the
typed Rust read-only bounds query; scheduler-policy selection, C process APIs,
and `crabc-libc` remain unadmitted.

The separate `./scripts/dev-x86_64.sh rr-interval-reference` command records
the direct typed x86 read-only `sched_rr_get_interval(2)` query. It pins
syscall 148, the 16-byte align-8 `timespec`, PID-zero and explicit-`gettid`
selection for both the calling task and a distinct live worker task, canonical
nonnegative duration validation, and direct `ESRCH` propagation. Its
pinned-musl C oracle uses that worker only as harness machinery; it does not
select a C API, pthread facade, scheduler policy selection/mutation, other
scheduler parameter query, errno TLS, or CPU-affinity support.

The separate `./scripts/dev-x86_64.sh priority-reference` command pins x86
`PRIO_PROCESS`/`PRIO_PGRP`/`PRIO_USER`, `getpriority` syscall 140, its
non-negative `[1, 40]` success encoding, and missing-process `ESRCH` behavior.
It is evidence only for the typed Rust read-only `getpriority` slice; it does
not itself admit priority mutation, C process APIs, or `crabc-libc`.

The separate `./scripts/dev-x86_64.sh setpriority-reference` command executes
a child-contained pinned-musl x86 scheduling-priority lifecycle. It pins
`setpriority=141`, the existing closed `PRIO_PROCESS`/`PRIO_PGRP`/`PRIO_USER`
selectors and `[-20, 19]` nice vocabulary, raw set plus musl read, musl no-op
set plus raw read, invalid-selector `EINVAL`, and missing process/group/user
target `ESRCH`. It
admits typed `process::{setpriority_process, setpriority_process_group,
setpriority_user}` only; scheduler-policy mutation and C process APIs remain
excluded.

The separate `./scripts/dev-x86_64.sh rlimit-reference` command executes a
pinned-musl x86 read-only resource-limit lifecycle. It pins the 16-byte,
align-8 `rlimit`/`rlimit64` record (current/maximum offsets zero and eight),
`prlimit64=302`, `RLIM_INFINITY=UINT64_MAX`, selectors zero through 15,
repeated and explicit-self reads, and invalid-selector/missing-target errors.
It is evidence for typed calling-process `process::getrlimit`, with a PID-zero,
null-new-limit `prlimit64` query. Its explicit-self and missing-target
observations alone do not admit `process::getrlimit_for`; target mutation,
C process APIs, and general x86 facade support remain excluded.

The separate `./scripts/dev-x86_64.sh rlimit-targeted-reference` command
executes a pinned-musl/raw x86 read-only targeted resource-limit query. A
forked live child holds a distinct safe `RLIMIT_NOFILE` soft limit while musl
`prlimit` and raw `prlimit64=302` both return its complete 16-byte, align-8
`rlimit64` record. The native Rust regression performs the matching typed
optional-PID query and preserves missing-PID `ESRCH`. It admits the bounded
read-only `process::getrlimit_for` slice only; target mutation, C process APIs,
errno TLS, and the broader record-owning family remain excluded.

The separate `./scripts/dev-x86_64.sh setrlimit-reference` command executes a
child-contained pinned-musl x86 calling-process resource-limit lifecycle. It
pins `prlimit64=302`, the same 16-byte `rlimit64` record, raw set plus musl
read, musl restore plus raw read, and inverted-limit `EINVAL`. It admits only
typed `process::setrlimit` for PID zero; target-process mutation and C process
APIs remain excluded.

The separate `./scripts/dev-x86_64.sh rusage-reference` command executes a
pinned-musl x86 read-only resource-usage lifecycle. It pins the 16-byte,
align-8 `timeval`, the 144-byte, align-8 initialized `rusage` kernel prefix
(selected offsets zero, 16, 32, and 136), musl's 272-byte public record with
its reserved tail at offset 144, `getrusage=98`, selectors `0`/`-1`/`1`,
direct selector observations with stable children-prefix equivalence,
canonical observations, and invalid-selector behavior. It admits typed
read-only `process::getrusage` only: the Rust value copies Linux's initialized
kernel prefix and omits musl's uninitialized reserved tail. C `struct rusage`
support, raw storage exposure, broader process-accounting policy, and general
x86 facade support remain excluded.

The separate `./scripts/dev-x86_64.sh times-reference` command executes a
pinned-musl x86 read-only process-accounting lifecycle. It pins signed 64-bit
`clock_t`, the 32-byte, align-8 `tms` record (field offsets zero, eight, 16,
and 24), `times=100`, nonnegative process-accounting fields, a normal
nondecreasing observation sequence, and direct syscall observations. Its
independent elapsed-tick result remains signed because it may wrap. It admits
only typed read-only `process::times`, whose values stay in opaque clock ticks;
C `times`/`struct tms` support, tick-rate conversion, broader accounting
policy, and general x86 facade support remain excluded.

The separate `./scripts/dev-x86_64.sh fstat-reference` command records the
pinned-musl x86 144-byte `fstat(2)` record and regular-file behavior. It
supports only the typed descriptor `fs::fstat` slice; the broader
`filesystem.path-core` capability remains a promotion obligation.

The separate `./scripts/dev-x86_64.sh ftruncate-reference` command records
the `ftruncate` portion of the direct x86 file-position family. It pins
`ftruncate=77` and the signed 64-bit Linux `loff_t` argument, and uses a
fresh owned memfd to prove extension with a zero-filled new range followed by
shrink. The typed Rust `u64` boundary rejects values above `i64::MAX` with
`EINVAL` before borrowing the descriptor or issuing the syscall.

The separate `./scripts/dev-x86_64.sh timestamp-reference` command records
the bounded x86 timestamp-mutation family. It pins `utimensat=280`, the
`rdi`/`rsi`/`rdx`/`r10` syscall4 argument placement, and the signed 16-byte
align-8 `timespec` pair used by
`fs::{Timespec, Timestamps, UTIME_NOW, UTIME_OMIT, futimens}`. The single
reference covers descriptor/null-path timestamps, bounded directory-relative
and current-directory forms, final-symlink no-follow mutation, and the legacy
whole-second form. Explicit/current/omit values, direct kernel validation, and
the legacy conversion boundary are native evidence. It completes only
`filesystem.fd-timestamps`, `filesystem.directory-relative-timestamps`,
`filesystem.cwd-timestamps`, `filesystem.symlink-timestamps`, and
`filesystem.second-resolution-timestamps`; general `filesystem.path-core`,
public C timestamp APIs/errno TLS, and broader metadata policy remain
excluded.

The separate `./scripts/dev-x86_64.sh posix-fallocate-reference` command
records only typed `fs::posix_fallocate`: x86 `fallocate=285`, signed 64-bit
`off_t`, fixed mode zero, an unlinked regular-file extension to 8192 bytes,
retained prefix/zero-filled newly allocated range, and preserved shared file
position. The pinned-musl C spelling returns direct
`EINVAL`/`EBADF` values without changing `errno`, while the raw syscall uses
`-1`/`errno`; Rust returns `Errno` and rejects unsigned offset, length, and
half-open-range values that do not fit `loff_t` before borrowing the
descriptor. It completes only `filesystem.posix-allocate-range`, not general
Linux fallocate flags, pathname allocation, C/errno-TLS behavior, filesystem
fallback/policy, durability, or the separate `filesystem.allocate-range`
capability.

The separate `./scripts/dev-x86_64.sh fallocate-reference` command records
general typed `fs::fallocate` evidence: x86 `fallocate=285`, signed 64-bit
`off_t`, and the closed `ALLOCATE=0`, `KEEP_SIZE=0x01`, `PUNCH_HOLE=0x02`, and
`ZERO_RANGE=0x10` modes. Pinned-musl/raw fixtures prove extension, keep-size,
stable descriptor position, invalid combinations and unknown bits,
zero-length/negative-range errors, read-only and closed-descriptor errors, and
pipe `ESPIPE`. Where the fixture filesystem supports zero-range or punch-hole,
it also proves their retained-edge and zeroed-range effects; otherwise both C
paths must return `EOPNOTSUPP` without changing size or position. Safe Rust
preflight rejects unknown modes, invalid combinations, and unrepresentable
unsigned ranges before borrowing the descriptor. C `fallocate` and the raw
syscall use `-1`/`errno`; C ABI and errno TLS are excluded. Future flags,
pathname allocation, filesystem fallback/policy, durability, and public x86
support remain outside this promotion.

The separate `./scripts/dev-x86_64.sh file-position-reference` command
completes that direct family with `lseek=8`, `fsync=74`, and
`fdatasync=75`; signed 64-bit `off_t`; `SEEK_SET`/`SEEK_CUR`/`SEEK_END`
and sparse `SEEK_DATA`/`SEEK_HOLE` positions; position-preserving sync
requests; direct oversized `SEEK_SET`
`EINVAL` plus `SEEK_DATA`/`SEEK_HOLE` `ENXIO`; pipe `ESPIPE`; and
invalid-descriptor `EBADF` behavior. Together
the two commands establish typed
`fs::{seek, tell, ftruncate, fsync, fdatasync}`, but not C
`unistd`/header behavior, pathname truncation, allocation, file-allocation
policy, or a host-filesystem durability guarantee.

The separate `./scripts/dev-x86_64.sh sync-reference` command admits only
typed `fs::sync` system-wide filesystem synchronization. It records x86
syscall `162`: after a disposable dirty regular-file fixture, pinned musl's
void wrapper returns normally and the raw syscall returns zero. This proves
Linux's kernel/filesystem writeback completion request and specified
unit-success contract, not writeback timing, power-loss or storage-cache
durability. It does not admit the distinct descriptor-associated `syncfs(2)`
operation, C filesystem APIs, pathname opening, or broader filesystem
behavior.

The separate `./scripts/dev-x86_64.sh syncfs-reference` command admits only
typed `fs::syncfs` descriptor-associated filesystem synchronization. It records x86 syscall
`306` over one borrowed descriptor argument: pinned musl and the raw syscall
both accept the regular-file fixture and a pipefs descriptor, preserve the
regular-file position, and return `EBADF` after a duplicated descriptor is
closed. Its successful return is a kernel/filesystem writeback completion
point, not a power-loss or storage-cache durability guarantee. It does not
admit the distinct process/system-wide `sync(2)` operation, C filesystem APIs,
pathname opening, or broader filesystem behavior.

The separate `./scripts/dev-x86_64.sh sync-file-range-reference` command
admits only typed `io::{sync_file_range, SyncFileRangeFlags}` writeback
requests. It combines pinned-musl/raw evidence for x86 syscall `277`,
`rdi`/`rsi`/`rdx`/`r10` argument order, signed 64-bit `loff_t`, and
`WAIT_BEFORE`/`WRITE`/`WAIT_AFTER` values `1`/`2`/`4` with the focused Rust
range-boundary regression. A regular-file zero-length-to-EOF request may
succeed or report direct `EOPNOTSUPP` and leaves the descriptor position
unchanged; the raw/pinned-musl pair also checks unknown-flag `EINVAL`, pipe
`ESPIPE`, and invalid-descriptor `EBADF`. The safe facade rejects unknown
flags and unsigned values that cannot represent Linux's signed range, while a
live raw-core invalid-flag check proves the fourth x86 syscall argument is
delivered in `r10`; a closed descriptor is necessarily exercised at that raw
seam because a safe borrowed descriptor cannot outlive its open handle. This
is a writeback request only: it does not claim metadata or storage-cache
durability, select C `sync_file_range`, pathname opening, or broader
filesystem behavior.

The separate `./scripts/dev-x86_64.sh statat-reference` command records the
private x86 144-byte record through `newfstatat(2)`, both relative to a
borrowed directory descriptor and through `CWD`, with only
`AT_SYMLINK_NOFOLLOW`. It remains the narrow `st_dev`/`st_ino` identity
foundation for the separately admitted logical current-directory name:
`AT_EMPTY_PATH`, general stat/path APIs, and filesystem mutation remain out of
scope.

The separate `./scripts/dev-x86_64.sh access-reference` command completes the
record-free direct `fs::{access, accessat}` permission-observation slice. It
pins x86 `access=21`, legacy three-argument `faccessat=269`, and
flags-bearing `faccessat2=439`; the Rust `access` spelling intentionally uses
the legacy `faccessat(AT_FDCWD, path, mode)` seam. The closed `Access` mode
vocabulary admits only `F_OK`, `R_OK`, `W_OK`, and `X_OK`, while a distinct
`AccessAtFlags` type admits only `AT_EACCESS` and
`AT_SYMLINK_NOFOLLOW`, preventing those access-only bits from widening the
private `statat` flag boundary. Pinned-musl and raw calls agree on ordinary and
descriptor-relative checks, missing-path `ENOENT`, dangling-final-symlink
follow versus nofollow policy, and invalid mode/flag `EINVAL`. A short-lived
root child makes real and effective UIDs differ, proving that ordinary checks
use the real identity while `AT_EACCESS` uses the effective identity. The
fixed-stack Rust path boundary rejects an interior NUL and 256-byte byte input
before the syscall. This does not admit pathname mutation, allocation-backed
path helpers, C APIs or errno TLS, or the broader `filesystem.path-core`
family.

The separate `./scripts/dev-x86_64.sh getcwd-reference` command completes
the physical `filesystem.cwd` observation and the contained
`filesystem.path-metadata` logical-name slice. Direct syscall 79 and pinned
musl agree on the exact initialized NUL-terminated caller-buffer prefix and
undersized-buffer `ERANGE`. The pinned-musl C wrapper instead reports `EINVAL`
for a zero-size input; the direct Rust facade deliberately retains the
raw-kernel `ERANGE` rather than emulating that wrapper policy. The alloc-gated
`process::getcwd_alloc` clears and reuses a caller vector, retries only
`ERANGE`, and returns a `CString`; a native child regression puts its CWD
beyond the initial small buffer.

The same gate records raw and pinned-musl `newfstatat=262` agreement over the
x86 144-byte `stat` layout's `st_dev`/`st_ino` identity fields. Pinned
musl's C `get_current_dir_name` reads environment `PWD`; the Rust APIs instead
take an explicit caller-owned `Option<&CStr>` snapshot and never read the
environment. A nonempty absolute snapshot whose identity matches `.` preserves
its exact logical (including non-UTF-8) spelling. Mismatched, relative, empty,
and absent snapshots fall back to physical `getcwd`; a too-small validated
buffer reports `RANGE`. The alloc-gated form likewise returns either the exact
logical spelling or physical fallback. `chdir`, `fchdir`, C APIs, errno TLS,
general stat/path APIs, and general x86 facade support remain excluded.

The separate `./scripts/dev-x86_64.sh readlinkat-reference` command records the
private x86 caller-buffer-only `readlinkat(2)` target query. The caller owns
writable output storage; Linux returns an initialized target prefix without a
NUL byte, and a short output buffer succeeds with its truncated prefix. The
raw syscall rejects a zero-length buffer with `EINVAL`, while pinned musl's C
wrapper returns an empty successful result; the direct Rust facade deliberately
retains the kernel result. `&str` and byte-slice paths use the fixed 256-byte
stack conversion buffer, while a borrowed `&CStr` remains caller-owned.
Allocation-backed readlink helpers, general pathname APIs, and filesystem/path
mutation remain deferred, so this evidence does not promote
`filesystem.path-core` or widen the contained logical-current-directory
metadata slice.

The separate `./scripts/dev-x86_64.sh system-reference` command records the
pinned-musl x86 `uname(2)` and `sysinfo(2)` behavior used by the typed
`system::{uname, sysinfo, load_average}` slice. It is neither public C
system-information support nor a `crabc-libc` selection.

The separate `./scripts/dev-x86_64.sh sched-affinity-reference` command
records the direct typed x86 read-only CPU-affinity observation slice. It pins
the 128-byte native mask and syscall 204, PID-zero and explicit `gettid`
selection for both the calling task and a distinct live non-leader task, and
the transient-snapshot boundary. The raw syscall returns its dynamic
initialized-prefix length while leaving its tail untouched; pinned musl's C
wrapper returns zero on success and clears the rest of `cpu_set_t`. The typed
Rust facade owns a zeroed mask, exposes no raw length or C return value, and
does not allocate, retry, or truncate. Its pinned-musl pthread worker is
oracle harness machinery only; this observation gate excludes scheduler
policy, C or pthread facades, and errno TLS.

The separate `./scripts/dev-x86_64.sh sched-affinity-set-reference` command
records the direct typed x86 `sched_setaffinity(2)` mutation slice. It pins the
128-byte mask and syscall 203. The probe reapplies the initial task's observed
mask without broadening it, then selects a retained live non-leader worker task
by explicit `gettid` and narrows that worker to one observed CPU before it
exits. The typed facade accepts a caller-provided bounded `CpuSet`, which Linux
may intersect with available and cgroup-permitted CPUs. Both musl and the raw
syscall succeed; an empty mask returns `EINVAL`, a missing task ID returns
`ESRCH`, and the resulting mask cannot contain a CPU outside the requested one.
The pinned-musl pthread worker is oracle harness machinery only. Other
scheduler policy, C or pthread facades, errno TLS, and the broader
record-owning family remain excluded.

The separate `./scripts/dev-x86_64.sh thread-reference` command records
pinned-musl x86 `gettid`, `sched_getcpu`, and `sched_yield` behavior for the
bounded typed thread-observation slice. It does not establish pthread,
affinity, scheduling-policy, or C thread API support.

The separate `./scripts/dev-x86_64.sh thread-credentials-reference` command
records pinned-musl x86 `setresuid=117` and `setresgid=119`, unsigned 32-bit
`uid_t`/`gid_t`, and musl/raw all-ones no-change state preservation. The direct
typed Rust boundary maps only `None` to Linux's all-ones no-change word and
rejects an explicit typed all-ones ID with `EINVAL`. It deliberately preserves
the raw syscall's calling-task scope rather than emulating musl's process-wide
synchronized credential transition, so it neither establishes C credential
APIs nor promotes broader process/thread behavior.

The separate `./scripts/dev-x86_64.sh fs-credentials-reference` command
records pinned-musl x86 `setfsuid=122` and `setfsgid=123`, unsigned 32-bit
identity words, all-ones query semantics, and prior-identity returns. Its
short-lived child compares raw and musl queries, then requests the current
effective UID/GID through both boundaries. The typed unsafe Rust API exposes
only `process::{set_fs_uid, set_fs_gid}`: `None` is the sole all-ones query
form, explicit typed all-ones IDs are rejected with `EINVAL`, and a requested
change cannot report permission denial because Linux still returns the prior
identity. It does not establish C credential APIs, musl's process-wide
credential synchronization, or broader process/thread behavior.

The first native Linux/x86-64 core boundary is implemented and checked through
the deliberately closed `./scripts/dev-x86_64.sh core` runner. Its evidence is
the `crabc-core` `x86_64-unknown-linux-musl` lib suite, which directly covers
the x86 syscall argument registers through `openat` mode and six-word futex
behavior, `%fs` thread-pointer identity, x86 ELF/vDSO discovery including
`__vdso_gettimeofday`, x87 plus MXCSR floating-point-environment updates, and
a raw `rt_sigaction`/`SA_RESTORER` install, exact-thread delivery, and
`rt_sigreturn` round trip.

The separate `./crt/run-x86_64.sh static-pie` runner verifies the deliberately
bounded static-PIE CRT foundation: `rcrt1.o`, `crti.o`, and `crtn.o` apply
checked `R_X86_64_RELATIVE` RELA/RELR records, seal GNU RELRO, enter the
static lifecycle, and reject a mutated non-relative relocation. It does not
provide dynamic `crt1.o`/`Scrt1.o`, sysroot installation, or a loader handoff.

The separately fixed `./scripts/dev-x86_64.sh ldso-relocation` command runs
the source-only `ldso/src/x86_64_relocation.rs` test module under native
Linux/x86-64. It proves checked symbol-free `R_X86_64_RELATIVE` RELA and ELF64
RELR processing, including complete preflight, table-target separation, and
no-mutation rejection of duplicate targets. It is not selected by
`crabc-ldso`, does not provide an ELF interpreter or symbol lookup, and does
not relax the dynamic-loader completion obligation below.

The separate `./scripts/dev-x86_64.sh ldso-image` command runs the source-only
`ldso/src/x86_64_image.rs` suite under native Linux/x86-64. It validates an
ELF64 little-endian x86 image's program-header map, load ranges, dynamic
metadata, and RELA/RELR descriptors before the relocation foundation consumes
them. It does not map an image, select an ELF interpreter, resolve symbols, or
select `crabc-ldso`.

The closed `./scripts/dev-x86_64.sh libc-syscall` command independently
compiles the unintegrated `libc/src/c_abi/x86_64/syscall.rs` source and
behavior-tests raw `openat`, `setsockopt`, and `mmap` calls through all six
x86 syscall argument registers. It establishes neither a `crabc-libc` binary
nor any C layout, errno, TLS, pthread, or header support.

The closed `./scripts/dev-x86_64.sh libc-errno-tls` command independently
compiles `libc/src/c_abi/x86_64/errno.rs` and links a native C fixture
through the installed project `errno.h`. It proves a local initial-TLS datum
with `R_X86_64_TPOFF*`, no `__tls_get_addr` resolver path, zero initialization,
and isolated main/pthread `errno` slots. This remains one source-only C leaf:
it does not select `crabc-libc` or establish complete C layouts, pthread
behavior, headers, or syscall-to-errno translation, and is not a musl
differential or compatibility-oracle gate.

The closed `./scripts/dev-x86_64.sh libc-thread-pointer` command independently
compiles the private `libc/src/c_abi/x86_64/thread_pointer.rs` leaf. It maps
pinned musl 1.2.6 `arch/x86_64/pthread_arch.h::__get_tp()`'s direct `%fs:0`
read and compares its opaque candidate word with a pinned-musl inline fixture
in both a main thread and a real worker. It neither dereferences that word nor
requires it to be nonzero. The leaf does not perform
`__pthread_self()`/`TP_OFFSET` arithmetic, derive `errno`, initialize or
allocate TLS, write an FS base, expose `pthread_t`, or establish pthread
creation/join, clone-TLS handoff, cancellation, static/dynamic/loader TLS,
`__tls_get_addr`, `pthread_self`, `__errno_location`, public C ABI support,
`crabc-libc`, CRT, ldso, or sysroot support.

The closed `./scripts/dev-x86_64.sh libc-foundation` command compiles one
source-only x86 C-runtime primitive-composition object. It keeps the existing
raw syscall and initial-TLS errno leaves target-specific, then proves their
fixed-six-word private bridge to errno-publication path alongside the
already-proved fixed-musl memory and fenv symbols. It deliberately does not
export public C `syscall(long, ...)`, whose variadic contract remains a
selected-runtime obligation. The same focused C fixture executes once against
pinned musl and once against the isolated project object with project
`errno.h`, `sys/syscall.h`, `string.h`, and `fenv.h` first. This does not
select `crabc-libc`, widen any C/POSIX, pthread, signal, loader, CRT, or
sysroot claim, or alter public x86 support.

The closed `./scripts/dev-x86_64.sh libc-fenv` command independently compiles
the fixed-musl `libc/src/c_abi/x86_64/fenv.rs` leaf, then executes the same C
fixture against pinned musl and the isolated project object with project
`<fenv.h>` first. It proves the x86 32-byte x87/MXCSR `fenv_t` layout,
exception-flag transitions, four rounding modes, saved/updated environments,
and `FE_DFL_ENV`. This is still a source-only C layout/behavior leaf:
`crabc-libc` does not select it, and no broad C fenv, C runtime, or x86
public-support claim follows from the checkpoint.

The closed `./scripts/dev-x86_64.sh libc-memory` command independently
compiles the fixed-musl `libc/src/c_abi/x86_64/memory.rs` leaf, then executes
the same C fixture against pinned musl and the isolated project object with
project `<string.h>` first. It proves the exact x86 `memcpy`, `memmove`, and
`memset` kernels across alignment/length matrices, overlapping moves,
guard-page edges, return values, and direction-flag restoration. This remains
a source-only C string/memory leaf: `crabc-libc` does not select it, and no
general C runtime or x86 public-support claim follows from the checkpoint.

The closed `./scripts/dev-x86_64.sh libc-setjmp` command independently
compiles the unintegrated `libc/src/c_abi/x86_64/setjmp.rs` leaf and links a C
fixture through the staged project `<setjmp.h>`. It differentially executes
the same fixture against pinned musl, then proves the x86 200-byte
machine/signal-mask record, register and stack restoration, direct aliases,
zero-to-one `longjmp` conversion, and `sigsetjmp` mask restoration. This is
still source-only evidence: `crabc-libc` does not select it and no broad C
runtime or x86 public-support claim follows from the checkpoint.

The closed `./scripts/dev-x86_64.sh libc-atomic` command independently compiles
the unintegrated `libc/src/c_abi/x86_64/atomic.rs` leaf and executes its
native behavior/disassembly probe. It proves only locked i32 `cmpxchg`, `xchg`,
and `xadd` helper behavior under the stated atomic-storage contract. The leaf
is not selected by `crabc-libc` and does not establish pthread, TLS, or C ABI
parity.

The closed `./scripts/dev-x86_64.sh libc-clone-raw` command independently
compiles a uniquely named private lexical port of musl 1.2.6's
`src/thread/x86_64/clone.s`. Its paired pinned-musl/candidate fixture proves
the `SIGCHLD` process-clone callback, supplied child stack, and exit-status
tail, while object inspection rejects public `clone`/`__clone`, runtime, and
dynamic-TLS dependencies. It does not expose public C `clone`, pthread, or
TLS behavior.

The closed `./scripts/dev-x86_64.sh libc-signal-foundation` command
independently compiles the private record-conversion portion of musl 1.2.6's
`sigaction` path. It pairs the 152-byte public x86 action with the 32-byte
kernel record, first mask word, `SA_RESTORER`, and hidden syscall-15 restorer,
without installing or delivering a signal. It proves neither public C
`sigaction`/`signal` behavior nor a `crabc-libc` artifact.

The direct `crabc-rs` foundation admits record-independent modules
(`buffer`, `collections`, `fd`, `fenv`, `ffi`, `io`, `ioctl`, `memory`,
`numeric`, scalar `param` observations, `pipe`, `rand`, `stdio`, `text`,
typed `fs::flock` whole-file advisory locking plus typed `fs::sendfile`
descriptor transfer plus typed `fs::sync`
system-wide filesystem synchronization plus typed
`fs::syncfs` descriptor-associated filesystem synchronization plus
`io::{sync_file_range, SyncFileRangeFlags}` range-writeback requests, and a
closed-mode `fs::{FallocateFlags, fallocate}` range-allocation slice, the
bounded timestamp-mutation family headed by
`fs::{Timespec, Timestamps, UTIME_NOW, UTIME_OMIT, futimens}` with named
directory-relative, current-directory, final-symlink, and whole-second forms,
and a closed `time` clock-query/whole-second-observation/relative-sleep,
`clock_nanosleep`, read-only interval-timer-query, and process-interval-control
slices), the scalar
`eventfd2` counter API, closed typed `poll(2)`, `ppoll(2)`, and `pause` slices,
the exact 144-byte descriptor `fs::fstat` record plus typed
`fs::{access, accessat, fadvise, readahead, seek, tell, ftruncate, fsync,
fdatasync, sync, syncfs, utimensat, futimens, futimes, futimesat, lutimes,
utimes, utime}`, and separately private `fs::{statat, stat,
readlinkat_raw}`
identity/readlink slice, plus direct typed
`process::{getcwd, getcwd_alloc, get_current_dir_name, get_current_dir_name_alloc}`
physical/logical current-directory observations, read-only typed process identity/process-group/
session observations plus `pidfd_open`, typed calling-process `setrlimit`, and
the process-global `umask` exchange, `getpriority`, typed
`setpriority_{process,process_group,user}`, `fcntl_getlk`,
`fs::{OFlags, fcntl_getfl, fcntl_setfl, FlockOperation, flock, sendfile}`, and
`scheduler_priority_bounds`,
typed `process::{getgroups_count, getgroups}` supplementary-group query/fill,
unsafe calling-task `process::{set_fs_uid, set_fs_gid}` query/current-effective
identity requests,
`thread::{gettid, sched_getcpu, sched_yield, set_thread_res_uid,
set_thread_res_gid}` and borrowed-atomic `thread::futex::{wait, wake}`, plus
the direct `thread::{sched_rr_get_interval, sched_getaffinity,
sched_setaffinity}` operations, bounded
`system::{uname, sysinfo, load_average}`, a closed
`mm::{mmap, mmap_anonymous, mremap, mremap_fixed, mprotect, munmap, mlock,
mlock_with, munlock, msync, madvise, posix_madvise, mincore}` slice, native
`getrandom`/`getentropy` and owned `RandomState`, and the separately proved x86
signal-action/raise boundary. The mapping slice permits `PROT_NONE` (the empty
set) plus only `READ`/`WRITE`/`EXEC` protections and exactly one `PRIVATE` or
`SHARED` mapping mode; creation rejects `MAP_32BIT`, `MAP_FIXED`,
`MAP_FIXED_NOREPLACE`, other fixed mappings, architecture-specific protection
bits, and all wider VM policy. Per-range locking admits only `mlock`,
`mlock2(MLOCK_ONFAULT)`, and `munlock`, preserving direct constrained-memlock
errors; it excludes global locking policy. Mapping synchronization exposes the
kernel-validated `MS_*` flags. Advice exposes only normal/random/sequential/
will-need plus separately named Linux page-discarding `MADV_DONTNEED`; POSIX
`DONTNEED` remains the musl-compatible no-op and does not forward an unaligned
range to Linux. Residency writes only the required `ceil(len / 4096)` bytes of
caller-owned output and leaves a supplied suffix untouched.
Remapping admits only `MREMAP_MAYMOVE`; the separate unsafe `mremap_fixed` form
binds fixed-destination replacement to an explicit destination, while
`MREMAP_DONTUNMAP` remains deferred. `param` does not yet expose `AT_EXECFN` as a
safe pathname reference: a raw auxv word alone does not prove pointer
provenance. Native evidence covers x87/MXCSR fenv state, direct descriptor/pipe
I/O including Linux/x86-64's `O_DIRECT` packet-mode bit, vector and positioned
I/O through the 64-bit offset split, `preadv2`/`pwritev2` current-offset and
flag rules, descriptor duplication and `fcntl` flags, ioctl encoding and typed
`EBADF`, stable scalar aux-vector observations, eventfd `NONBLOCK`/`CLOEXEC`
counter reset and semaphore behavior, a regular-file `fstat` record, private
descriptor-relative/CWD `statat` metadata, direct `access`/`accessat`
real/effective-credential observations, typed filesystem access advice/readahead
with stable descriptor position, typed seek/tell plus
ftruncate extension/zero-fill/shrink and position-preserving fsync/fdatasync,
typed descriptor/null-path, directory-relative, current-directory,
final-symlink, and whole-second timestamp mutation through `utimensat=280`
with its signed 16-byte `timespec` pair and direct validation (not general
pathname mutation, a C timestamp API, or errno TLS),
typed `fs::sync` system-wide kernel/filesystem writeback completion with its
unit-success contract (not timing, storage-cache, or power-loss durability),
typed `fs::syncfs` regular-file and pipefs acceptance with stable regular-file
position and raw-core closed-descriptor `EBADF` (not storage-cache durability
or process/system-wide `sync(2)`),
typed `fs::flock` whole-file advisory locks with duplicate open-file-
description sharing/release and independently opened nonblocking contention
(not `fcntl` record locks, durability, or network-filesystem semantics),
typed `fs::sendfile` descriptor transfer with optional explicit input offsets
or shared-position advancement, short/EOF results, and direct errors (not
socket/network or splice behavior, pathname opening, C APIs, or durability),
typed `fs::copy_file_range` descriptor-range copying with independently
optional explicit offsets staged until successful kernel completion or
shared-position advancement for null offsets, short/EOF results, fixed zero
flags, raw/pinned-musl negative-offset `EOVERFLOW`, nonzero-flag `EINVAL`,
and closed-descriptor `EBADF`, plus typed unsigned-range preflight `EINVAL`
(not C APIs or errno TLS, pathname operations, copy flags, sendfile/splice
fallbacks, filesystem-copy policy, or durability),
the distinct typed `io::sync_file_range` signed-range/closed-flag writeback
request (not metadata or storage-cache durability), typed
process identity/session observations plus a nonblocking owned pidfd,
read-only `getpriority`, typed scheduling-priority mutation, conflicting-lock
`F_GETLK` record, and scheduler-priority bounds, read-only RR interval
observations, bounded
direct CPU-affinity observation/mutation, thread ID/CPU/yield
observations plus borrowed-atomic futex mismatch/timeout/wake/exchange,
uname/sysinfo/load observations, anonymous
mapping protection transitions, remap growth/shrink and fixed-destination
replacement, a sparse file mapping at a full 64-bit 4 GiB offset, per-range
locking/locking-on-fault with constrained-memlock outcomes, mapping
synchronization, Linux anonymous-page discard versus POSIX advisory no-op, and
per-page residency output/rounding, raw random-source flags and
initialized-length behavior, musl's 256-byte `getentropy` boundary, and an
installed handler's x86 `rt_sigreturn` round trip, plus x86 `timespec` layout
and normalized realtime/monotonic/monotonic-raw/process-CPU query results plus
bounded whole-second realtime observation through the vDSO/direct-syscall
seam. The relative-sleep slice proves
zero-duration completion and signal-interrupted `nanosleep` with an explicit
positive remainder. The direct clock-sleep slice proves relative and absolute
`clock_nanosleep` pointer/error-mode contracts, including absolute `EINTR`
with no invented remainder. The
readiness slice proves
empty/readable/hangup pipe states, temporary `ppoll` signal-mask restoration,
and signal-only `pause` completion while retaining borrowed descriptor and
signal-set records. The separately executed direct readiness slice proves x86
12-byte epoll event records, close-on-exec creation, legacy-size validation,
future-bit forwarding for Linux validation, empty and pipe readiness, data-token
preservation, modification, deletion, initialized-prefix handling, and temporary
masked-wait installation/restoration through raw and pinned-musl probes. It also
proves the 1024-bit `fd_set`, direct select/pselect empty/readable and
invalid-`nfds` behavior, raw `pselect6` argument-six mask-pointer/size placement,
and raw/pinned-musl pselect mask restoration. This completes the bounded
`io.readiness` capability, not C polling support. The separately executed
timerfd slice completes direct typed
`time::{timerfd_create, timerfd_settime, timerfd_gettime}`: x86 32-byte timer
records, all five named clock values with alarm-clock capability results,
known and future flag forwarding, relative/absolute settings, `CANCEL_ON_SET`
acceptance, periodic-setting inspection, epoll readiness, exact expiration reads,
disarming, and invalid record/flag/descriptor behavior. The separately executed
statat slice proves the x86 144-byte record through
descriptor-relative and CWD lookup, final-symlink follow/no-follow behavior,
and closed path/flag validation. It is private evidence recorded under the
planned record-owning family, not direct-family promotion. The separately
executed read-only resource-limit query slice admits typed calling-process
`process::getrlimit`: it proves the x86 16-byte `rlimit64` record, closed
resource selectors, infinity mapping, and PID-zero/null-new-limit query. The
separate `./scripts/dev-x86_64.sh rlimit-targeted-reference` gate completes
only `process.resource-limits-targeted`: a forked live child retains a distinct
`RLIMIT_NOFILE` soft limit while pinned-musl `prlimit` and raw `prlimit64`
agree with its full record, and the native Rust test makes the matching
optional-PID query with missing-PID `ESRCH`. It does not admit target mutation,
C resource APIs, errno TLS, or the broader record-owning family. The separate
child-contained calling-process `setrlimit` slice completes only
`process.resource-limit-mutation`: it validates `current <= maximum` before
the PID-zero `prlimit64` call and does not admit target mutation, C resource
APIs, or the broader resource-limit family. The separately executed read-only
resource-usage slice admits typed read-only `process::getrusage`: it proves
the x86 initialized 144-byte `rusage` prefix, its 272-byte musl public-record
boundary, closed selectors, and canonical nondecreasing observations, while
copying only initialized fields into the Rust value. The separately executed
process-accounting slice
admits typed read-only `process::times`: it proves the x86 32-byte `tms`
record, nonnegative process tick fields, and its separately preserved signed
elapsed-tick return without exposing C storage or a tick rate.
The direct supplementary-group slice proves native `getgroups` count/fill
behavior, the `Gid` initialized-prefix output boundary, undersized-buffer
rejection, and the count-to-fill retry contract. The direct interval-timer
query proves native `getitimer` layout, closed selectors, canonical transient
results, and invalid-selector rejection without comparing snapshots that can
decrement. The `time.process-interval-control` slice proves `setitimer`
old/new/disarm exchange over validated microsecond durations for all selectors,
plus the Rust-only `alarm`/`ualarm` aliases on `ITIMER_REAL`; C `ualarm`
comparison is limited to subsecond inputs because musl does not normalize
inputs of one second or more. The identity slice checks native process IDs and
`/proc/self/status`
UID/GID triples. The direct
`fs::{memfd_create, fcntl_get_seals, fcntl_add_seals}` slice completes bounded
anonymous memory-file creation plus seal observation/mutation: fresh owned
descriptors, the closed creation-flag and stack-name boundaries, Linux-5.10
seal effects, and direct errors. It does not admit a C `fcntl` API,
`memfd_secret`, huge-page policy, executable policy, or broader filesystem
behavior. The direct
`fs::{OFlags, fcntl_getfl, fcntl_setfl}` slice completes typed
open-file-description status-flag observation/mutation across duplicates while
leaving pathname opening, generic C `fcntl`, and errno TLS unadmitted. The
direct
`fs::{seek, tell, ftruncate, fsync, fdatasync}` family now completes
`io.file-position`; direct `fs::sync` completes only
`filesystem.global-sync` as a system-wide kernel/filesystem writeback
completion request without timing or storage-cache/power-loss durability; and
direct `fs::syncfs` completes only `io.syncfs`, without completing broader
filesystem behavior; direct `fs::flock` completes only
`io.advisory-flock`, without expanding the separate `fcntl` record-lock
boundary; direct `fs::sendfile` completes only
`filesystem.descriptor-transfer`, without selecting socket/network or splice
semantics; direct `fs::copy_file_range` completes only
`filesystem.descriptor-range-copy`, with explicit offsets staged until success
and null offsets using shared positions, without selecting C APIs or errno TLS,
pathname operations, copy flags, sendfile/splice fallbacks, filesystem-copy
policy, or durability. Direct `fs::posix_fallocate` completes only
`filesystem.posix-allocate-range`: it fixes Linux `fallocate(2)` mode to zero
and preserves the descriptor position, without selecting general fallocate
flags, pathname allocation, C/errno-TLS behavior, filesystem fallback/policy,
durability, or the separate `filesystem.allocate-range` capability. The
direct `fs::fallocate` slice separately completes
`filesystem.allocate-range` for its closed modes, with no general flag,
pathname, C ABI, errno-TLS, fallback/policy, durability, or public-x86 claim.
direct `thread::{set_thread_res_uid, set_thread_res_gid}` family completes
only `thread.credentials-res`: its `None` no-change form and typed-sentinel
rejection retain raw Linux calling-task scope rather than musl's synchronized
process-wide credential transition. The
`fstat`, private `statat`, direct `access`/`accessat`, the named timestamp-
mutation family, direct `getcwd`/alloc-gated `getcwd_alloc`, and
file-access-advice slices do not
complete the broader
`filesystem.path-core` capability. Filesystem
behavior beyond those bounded operations, allocation-backed path-core helpers
or CWD mutation, readiness policy beyond the named select/pselect and epoll
operations, timerfd policy beyond the named typed descriptor operations,
signalfd,
mapping synchronization/advice/residency policy beyond the named operations and
global VM policy, networking, process/thread behavior beyond the named
observations (including target resource-limit mutation, scheduler-policy
mutation, or priority mutation beyond the named typed slice), time
calendars, broader interval-timer/other timer policy, timezones, broader clock
sleep/mutation and C sleep APIs, loader/runtime, and every other
AArch64-record-owning facade family remain absent until they have their own x86
ABI proof.

Its repeatable native evidence is deliberately fixed at
`./scripts/dev-x86_64.sh facade`, the direct
`./scripts/dev-x86_64.sh memfd-reference` and
`./scripts/dev-x86_64.sh ftruncate-reference` and
`./scripts/dev-x86_64.sh timestamp-reference` and
`./scripts/dev-x86_64.sh posix-fallocate-reference` and
`./scripts/dev-x86_64.sh fallocate-reference` and
`./scripts/dev-x86_64.sh file-position-reference` and
`./scripts/dev-x86_64.sh flock-reference` and
`./scripts/dev-x86_64.sh sendfile-reference` and
`./scripts/dev-x86_64.sh copy-file-range-reference` and
`./scripts/dev-x86_64.sh sync-reference` and
`./scripts/dev-x86_64.sh syncfs-reference` and
`./scripts/dev-x86_64.sh sync-file-range-reference` and
`./scripts/dev-x86_64.sh access-reference` and
`./scripts/dev-x86_64.sh fcntl-status-reference` and
`./scripts/dev-x86_64.sh setpriority-reference` and
`./scripts/dev-x86_64.sh rlimit-reference` and
`./scripts/dev-x86_64.sh rlimit-targeted-reference` and
`./scripts/dev-x86_64.sh rusage-reference` and
`./scripts/dev-x86_64.sh setrlimit-reference` and
`./scripts/dev-x86_64.sh umask-reference` and
`./scripts/dev-x86_64.sh times-reference` and
`./scripts/dev-x86_64.sh thread-credentials-reference` and
`./scripts/dev-x86_64.sh fs-credentials-reference` and
`./scripts/dev-x86_64.sh getgroups-reference` and
`./scripts/dev-x86_64.sh getitimer-reference` and
`./scripts/dev-x86_64.sh timerfd-reference` and
`./scripts/dev-x86_64.sh clock-nanosleep-reference` and
`./scripts/dev-x86_64.sh rr-interval-reference` and
`./scripts/dev-x86_64.sh sched-affinity-reference` and
`./scripts/dev-x86_64.sh sched-affinity-set-reference` and
`./scripts/dev-x86_64.sh epoll-reference` and
`./scripts/dev-x86_64.sh pselect-reference` and
`./scripts/dev-x86_64.sh getcwd-reference` gates, plus the separately named
`./scripts/dev-x86_64.sh statat-reference` and
`./scripts/dev-x86_64.sh readlinkat-reference`; none is a general x86 Cargo entry
point. The interval-timer-control and access-control evidence is part of the
admitted typed subset; statat/readlinkat remain private evidence and cannot
widen the admitted subset beyond the contained logical-current-directory
metadata slice.

These are only foundation checkpoints. In particular, they do not supply an
x86 C `fenv_t` ABI, `crabc-libc`, `crabc-ldso`, a complete CRT/sysroot, or a
generally admitted `crabc-rs` target; its bounded direct subset remains a
separate vertical slice and every omitted family remains a promotion
obligation below.

## Definition of parity

Parity means the same useful modern Unix runtime capability boundary that is
currently implemented and evidenced for Linux/AArch64. It is not a symbol
count, a C-wrapper count, or allocator-only parity. Each selected capability
must have an x86-64-native implementation, architecture-appropriate ABI proof,
and its own native evidence.

A completed x86-64 target includes:

- `crabc-core`: typed Linux kernel and vDSO primitives, x86-64 syscall ABI,
  thread-pointer/TLS access, atomics, futexes, signals, and architecture
  invariants.
- `crabc-libc`: the supported C/POSIX ABI, public headers and layouts, errno
  and TLS, pthread/TSD behavior, files/descriptors, process and signal
  behavior, memory mapping, time, stdio, locale/text/encoding, sockets,
  resolver, math, and the other currently supported AArch64 capability
  families.
- `crabc-ldso`: x86 ELF startup and finalization, relocation and symbol-scope
  rules, loader TLS, interpreter state, and the `RuntimeV1` bridge.
- CRT and sysroot artifacts: startup objects, compiler helpers, static and
  dynamic linking, static TLS, and source-build behavior that matches the
  current AArch64 contract.
- `crabc-rs`: idiomatic, typed native runtime operations with the same semantic
  accounting as AArch64. It must not become a mechanical C ABI wrapper layer.

The required observable capability families include descriptor and filesystem
operations, pipes, process creation and execution, credentials and controls,
signals, pthread/TLS, sockets, `mmap`, clocks and timers, stdio, resolver
behavior, dynamic linking, ABI/linkage, and the existing Rust-facade surface.

## Compatibility profile retained on x86-64

The x86-64 target retains the current AArch64 profile rather than widening it:

- locales are `C`, `POSIX`, and `C.UTF-8`; Rust text is UTF-8;
- resolver behavior is bounded to conventional `/etc` files and the documented
  A/AAAA/CNAME UDP/TCP retry/failover profile;
- no NSS/provider plugins, bundled tzdata, gettext framework, IDNA policy,
  async runtime, process supervisor, or security-policy framework;
- no hand-written cryptography; and
- musl 1.2.6 remains the C/POSIX oracle, Rustix remains a pinned native-API
  and test oracle, and glibc is never an oracle or fallback.

Existing native x86-64 `crabc-mimalloc` evidence is preserved as private
allocator evidence only. It does not establish a supported x86 C runtime,
loader, CRT/sysroot, Rust facade, or public x86 platform.

## Architecture-specific proof obligations

Before a capability is promoted, prove its x86-64-specific contract where
applicable: syscall numbers and calling convention; ELF machine, relocations,
code model, assembly, and linkage; TLS/thread-pointer model; atomic and futex
ordering; signal frames; `setjmp`/unwind/compiler helpers; data layout and
little-endian assumptions; CRT startup; and dynamic-loader behavior.

Each vertical slice follows: contract and capability classification, focused
test, implementation, direct ABI/boundary proof, musl/POSIX or other named
oracle evidence, then ledger and status documentation. Keep x86-64 ledgers and
reports separate from AArch64 measurements.

## Required execution order

1. Keep the parity ledger exhaustive and native-only, establish the pinned
   musl 1.2.6 oracle, then close architecture foundations: syscall register
   ABI, ELF/vDSO, thread pointer, floating-point state, signal frame/restorer,
   and target records.
2. Admit `crabc-rs` only family by family. A native compile is insufficient:
   every record passed to Linux must have its x86 layout and direct behavior
   proof before that API is exposed.
3. Port the C boundary in the same order: x86 syscall/assembly primitives,
   headers and layouts, errno/TLS and pthread state, then the selected C/POSIX
   families with musl 1.2.6 differential evidence.
4. Complete x86 CRT/sysroot and `crabc-ldso` as one bootstrapping chain:
   static and dynamic entries, relocations, TLS, loader handoff, and source
   builds. Do not promote a partial entry object as a sysroot.
5. Run the full native gate set and capability ledgers, then performance gates
   where the current AArch64 contract requires them. Only this final step can
   alter the public platform boundary.

## Promotion gates

Native x86-64 support can be declared only when all selected AArch64-equivalent
families pass their corresponding x86 evidence:

1. header, ABI, static-link, loader, TLS, signal, pthread, resolver, OS, and
   C/POSIX/libc-test gates;
2. `crabc-rs`, Rust-std-dependent, LTO, CRT/sysroot, static-TLS, and source
   build gates;
3. capability ledgers with no unaccounted selected surface; and
4. qualified performance evidence wherever the current AArch64 completion
   contract requires it.

Only then update the public scope/profile/status/dashboard documents to name
x86-64 as supported. Before then, describe this work as a future goal or
in-progress native evidence, never as current platform support.
