# Remaining completion gates

All implementation work from the preceding x86-64 remediation plan has been
completed locally. This file intentionally contains only work that cannot yet
be truthfully closed.

## Native evidence

- [ ] On the user-owned native AArch64 machine, run `./scripts/dev.sh closure`.
  Resolve every failed or invalid lane and preserve the AArch64 reports. Do
  not substitute emulation for this gate.

## Final record and user-owned hygiene

- [ ] Once both native closures are current and passing, regenerate the final
  dashboard and reconcile `SCOPE.md`, `COMPATIBILITY-PROFILE.md`, `STATUS.md`,
  `README.md`, and the architecture design documentation with the actual
  support boundary.

- [ ] Run the repository's user-owned formatting and applicable Clippy checks
  (the working contract assigns those commands to the user).

- [ ] Do not claim `STATUS: COMPLETE` until the preceding native evidence,
  documentation reconciliation, and user-owned hygiene evidence exist.
