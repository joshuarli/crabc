[package]
name = "crabc-core"
version.workspace = true
edition.workspace = true
description = "Internal typed Linux syscall primitives for AArch64 and staged x86-64 runtime evidence"
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "crabc_core"

[features]
default = []
# Retained for existing private allocator evidence manifests. Linux/x86-64
# core selection no longer depends on this feature; it does not by itself
# select libc, loader, CRT, or facade artifacts.
allocator-x86-evidence = []
