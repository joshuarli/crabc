[package]
name = "crabc-owned-cleanup-build-std"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
crabc-cleanup-dependency = { path = "../cleanup-dependency" }

[profile.release]
codegen-units = 1
debug = 0
incremental = false
lto = "fat"
panic = "unwind"
